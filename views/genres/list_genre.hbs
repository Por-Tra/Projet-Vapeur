<div class="page-title">
    <h1>Genres</h1>
</div>

{{#if listegenre}}
<ul class="list-simple">
    {{#each listegenre}}
        <li>
            <a href="/genres/{{idGenre}}/games">{{nomGenre}}</a>
        </li>
    {{/each}}
</ul>
{{else}}
<div class="card">
    <div class="card-body text-center">
        <p class="text-muted">Aucun genre pour le moment.</p>
    </div>
</div>
{{/if}}

<div class="mt-3">
    <a href="/" class="btn btn-ghost">← Retour à l'accueil</a>
</div>
